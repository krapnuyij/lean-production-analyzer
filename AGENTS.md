# AGENTS.md

## 프로젝트 목적

이 프로젝트는 제조업 생산혁신 / LEAN 직무 지원을 위한 1~2일 규모의 포트폴리오 프로젝트이다.

전체 분석 흐름:

`생산 데이터 → KPI 모니터링 → 병목 탐지 → Loss 분석 → 개선활동 → 개선 전/후 효과 측정`

기술적인 복잡성보다 다음을 우선한다.

* 명확한 비즈니스 로직
* 데이터의 논리적 일관성
* 분석 결과의 설명 가능성
* 제조 현장의 문제 해결 흐름

## 프로젝트 범위

* Python을 사용한다.
* 데이터 분석은 pandas, numpy 중심으로 구현한다.
* 대시보드는 추후 Streamlit, Plotly를 사용할 예정이다.
* 실제 기업 데이터가 아닌 합성 제조 데이터를 사용한다.
* 1~2일 안에 완성 가능한 범위를 유지한다.
* 명시적으로 요청하지 않는 한 ML을 추가하지 않는다.
* FastAPI, Docker, 데이터베이스, 인증 시스템 등 불필요한 인프라를 추가하지 않는다.
* overengineering을 피한다.

## 데이터 원칙

* 생산 변수 간 논리적 관계를 유지한다.
* 각각의 값을 완전히 독립적으로 random 생성하지 않는다.
* 가능한 KPI는 raw production data로부터 계산한다.
* 합성 데이터임을 명확히 문서화한다.
* 개선 전 `LINE-B / Stitching`이 주요 Bottleneck으로 나타나야 한다.
* Bottleneck과 개선 효과가 지나치게 완벽하거나 인위적으로 보이지 않게 한다.

## 분석 원칙

다음 흐름이 자연스럽게 드러나야 한다.

1. 전체 생산 KPI 확인
2. 성능이 낮은 Line 탐색
3. 해당 Line을 공정별로 Drill-down
4. Bottleneck 식별
5. Downtime 등 주요 Loss 원인 분석
6. 개선 전/후 KPI 비교
7. 개선 효과 정량화

## 작업 원칙

* 기존 프로젝트 구조와 코드를 먼저 확인하고 수정한다.
* 요청받은 범위에 필요한 변경만 수행한다.
* 요청하지 않은 다음 단계 기능을 임의로 구현하지 않는다.
* 구현 후 실제 실행과 검증을 수행한다.
* 실제 실행 결과를 보고한다.
* 단순한 문제에 과도한 class, abstraction, design pattern을 사용하지 않는다.

## Git 원칙

* 기존 Git 설정을 먼저 확인한다.
* global Git 설정은 임의로 변경하지 않는다.
* 기존 remote를 임의로 덮어쓰지 않는다.
* commit과 push는 사용자가 명시적으로 요청한 경우에만 수행한다.
* commit 메시지는 한국어로 작성한다.
* 코드 식별자, 라이브러리명, 도구명 등 전문 용어는 영어를 유지할 수 있다.
* `Co-Authored-By` 등의 attribution trailer를 추가하지 않는다.

## 응답 및 작업 보고

* 사용자에게 설명할 때는 한국어를 사용한다.
* 작업 완료 후 생성/수정한 파일과 주요 변경 사항을 요약한다.
* 테스트 또는 데이터 검증 결과를 실제 수치와 함께 보고한다.
* 문제가 발생하면 숨기거나 임의로 우회하지 말고 원인과 상태를 설명한다.
